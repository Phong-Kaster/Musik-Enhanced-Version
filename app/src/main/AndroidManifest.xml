<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.musik">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_musik"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_musik"
        android:supportsRtl="true"
        android:theme="@style/Theme.Musik"
        tools:targetApi="31">
        <activity
            android:name=".Homepage.HomeActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            tools:ignore="LockedOrientationActivity" />
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait"
            tools:ignore="LockedOrientationActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name=".Song.SongService"
            android:enabled="true"
            android:exported="false"/>

        <service android:name=".Song.MusicService"
            android:enabled="true"
            android:exported="false"/>
        <receiver android:name=".Song.MusicBroadcastReceiver"/>
    </application>

</manifest>